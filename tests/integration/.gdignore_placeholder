# Integration tests go here — one subdirectory per system
# Example: tests/integration/save_load/persistence_round_trip_test.gd
# Use for cross-system interactions, save/load round-trips, GymSys polling
