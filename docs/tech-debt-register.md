# Tech Debt Register

> Tracked advisory deviations from story close-outs. See `/tech-debt` for full analysis.

---

- **2026-05-30** (LootDropSystem autoload boot — Story 009): `var _config: LootRarityConfig = null` DI seam added to loot_drop_system.gd to allow tests to inject config without disk load. Story notes didn't explicitly list this seam. Consistent with project DI seam pattern (stat_system, ability_system, combat_resolver). No production impact. Effort: none (improvement). — tracked from `production/epics/loot-drop-system/story-009-autoload-boot-private-mode.md`

- **2026-05-30** (Formula E2 class affinity — Story 007): Implementation Notes specified CDF order [STRIKE, CONTROL, MOBILITY, NEUTRAL] but GDD AC-16 worked example (0.78 → NEUTRAL with STRIKE dominant) requires dynamic order: dominant → NEUTRAL → off-classes. Implementation correctly follows GDD; Implementation Notes had a spec authoring gap. If anyone re-reads Implementation Notes for E2, they should use the GDD §D Formula E2 worked example table as source of truth, not the story notes. Effort: fixed in source. — tracked from `production/epics/loot-drop-system/story-007-formula-e1-e2-e4.md`
- **2026-05-30** (Formula E4 MAX_INVENTORY — Story 007): AC-17 story text says "size=61→MAILBOX" (written when MAX_INVENTORY=60). GDD Pass 2 F-10 raised MAX to 120. Implementation Notes correctly say MAX=120. Story AC text is stale; future story authoring should sync AC text with Implementation Notes when Pass 2 changes are applied. Effort: none (documented, no code impact). — tracked from `production/epics/loot-drop-system/story-007-formula-e1-e2-e4.md`

- **2026-05-30** (Formula 3+4 TTL/bfcache — Story 005): `DropRevealState` enum was missing from Story 002's AC list (loot_enums.gd). Added in Story 005 as a necessary extension (Formula 4 bfcache_resume_action requires PRE_REVEAL/MID_REVEAL/POST_REVEAL_PRE_HANDOFF states). Story 002 completion notes should be updated to acknowledge this gap. Future story audits of loot_enums.gd should verify all required enums are present before dependent formula stories begin. Effort: none (resolved in Story 005). — tracked from `production/epics/loot-drop-system/story-005-formula-3-4-ttl-bfcache.md`

- **2026-05-30** (ceremony_cap_check — Story 004): `var current` renamed to `final_current`/`mini_current` to avoid GDScript same-scope redeclaration compile error. Story Implementation Notes had duplicate `var current` which would fail to compile — this is a spec authoring error, the fix is correct. If story notes are ever updated, use different variable names. Effort: fixed. — tracked from `production/epics/loot-drop-system/story-004-formula-2-ceremony-cap.md`
- **2026-05-30** (ceremony_cap_check — Story 004): `MINI_POOL_MAX_ENTRIES = 500` constant added to `loot_drop_system.gd` (story notes had magic literal 500 in housekeeping). Aligns with coding-standards data-driven rule. No downstream impact — value is consistent. Effort: none (improvement). — tracked from `production/epics/loot-drop-system/story-004-formula-2-ceremony-cap.md`

- **2026-05-30** (LootRarityCalc Formula 1 — Story 003): `LootRarityCalc` references `LootEnums.RarityTier` / `LootEnums.SourceEventKind` instead of `LootDropSystem.*` as story notes specified. Autoload stub has no enum re-exports yet. If Story 009 declares enums inside `LootDropSystem` and re-exports them, caller sites in `loot_rarity_calc.gd` (and its tests) need updating. Low risk — `LootEnums` is the canonical enum holder per Story 002 design. Effort: XS (search-replace if Story 009 adds re-exports). — tracked from `production/epics/loot-drop-system/story-003-formula-1-pillar1-proofs.md`
- **2026-05-30** (LootRarityCalc config path — Story 003): `_DEFAULT_CONFIG_PATH` in `loot_rarity_calc.gd` uses `res://assets/data/loot/loot_rarity_config.tres` (aligned to CI lint). If the canonical config path ever changes, update both the lint `CONFIG_FILE` and this constant in the same commit. Effort: XS. — tracked from `production/epics/loot-drop-system/story-003-formula-1-pillar1-proofs.md`

- **2026-05-30** (LootRarityConfig Resource — Story 002): `.tres` path is `assets/data/loot/loot_rarity_config.tres` (story spec said `data/loot/LootRarityConfig.tres`). Forced alignment to Story 001 CI lint `CONFIG_FILE` — the lint is the CI gate. If the path ever needs changing, update BOTH the lint `CONFIG_FILE` const AND the file location in the same commit. Effort: XS. — tracked from `production/epics/loot-drop-system/story-002-data-resources-enums.md`
- **2026-05-30** (LootRarityConfig SHA — Story 002): `entities.yaml` key is `loot_rarity_config_sha256`; story spec said `loot_rarity_config_sha`. Aligned to lint `PIN_KEY`. These are consistent between the lint and the registry. No action needed unless key name changes. Effort: XS. — tracked from `production/epics/loot-drop-system/story-002-data-resources-enums.md`
- **2026-05-30** (LootRarityConfig SHA — Story 002): SHA value is `"TBD"` placeholder in `entities.yaml`. Must be updated to the real SHA-256 of `assets/data/loot/loot_rarity_config.tres` after first Godot import (which re-serializes the .tres). Run: `godot --headless --script tools/ci/check_loot_config_hash_pinned.gd` after import — FAIL output will show the correct SHA. Then update entities.yaml and re-run to confirm PASS. Effort: XS. — tracked from `production/epics/loot-drop-system/story-002-data-resources-enums.md`

- **2026-05-30** (CI Lints — Loot Drop System Story 001): `check_loot_config_hash_pinned.gd` uses CONFIG_FILE path `res://assets/data/loot/loot_rarity_config.tres`. Story 002 must create `LootRarityConfig.tres` at this exact path, or update the lint's `CONFIG_FILE` const to match the chosen location. No impact while file is absent (lint exits 0 on missing target). Resolve before Story 002 commit. Effort: XS. — tracked from `production/epics/loot-drop-system/story-001-ci-lints-closed-api.md`

- **2026-05-30** (Stat Surface + StatSource Enum + Allow-List): `DEFAULT_BASE_VALUE = 10.0` and `DERIVED_PLACEHOLDER = 0.0` are hardcoded gameplay constants in `src/autoload/stat_system.gd`. Should be data-driven config per coding standards. Story 011 (mutation formulas) will replace with external config resource. — tracked from `production/epics/stat-system/story-002-stat-surface-enum-allow-list.md`
- **2026-05-30** (Mutation Formulas F1+F2): `PR_BASE = 6.0` in `stat_system.gd` is a PROVISIONAL VS-tier value pending ADR-0005 ratification (Q-A1 empirical cross-validation). Retune after ADR-0005 is Accepted; the const is schema-version-independent so retuning is a value change only (no migration). Story 013 (ADR-RATIFICATION-GATED) tracks the formal gate. — tracked from `production/epics/stat-system/story-011-mutation-formulas.md`
- **2026-05-30** (Combat Damage Tier): story-005 AC-16 had a typo (`[9→LIGHT, 400→HEAVY]`) that contradicts GDD Formula 4's `>=`-inclusive thresholds (9/1000=0.009<0.01→NEGLIGIBLE; 400/1000=0.40==T_CRITICAL→CRITICAL). Story text corrected; impl follows GDD. The GDD itself (`design/gdd/combat-resolver.md` AC-16) may carry the same typo — verify + sync at next `/consistency-check` or GDD revision. — tracked from `production/epics/combat-resolver/story-005-damage-tier-overkill.md`
- **2026-05-30** (GSM Suspended Gate + Reconciling): Reconciling re-read emits `stat_changed` with `StatSource.PR_BREAKTHROUGH` as a semantic stretch — the backend recorded the change, not a real PR. Consider adding a dedicated `RECONCILED` StatSource (would require StatSource enum expansion + allow-list + AC-04 test update). — tracked from `production/epics/stat-system/story-009-gsm-suspended-gate-reconciling.md`
- **2026-05-30** (Equipment Modifier Layer): AC-07 PL spy coverage is structural only (second-instance proves in-memory transience; `apply_equipment_modifier` has zero PL references). Full `PersistenceLayer.write()` spy assertion deferred to Story 004 when PL boot is wired. — tracked from `production/epics/stat-system/story-003-equipment-modifier-layer.md`
- **2026-05-30** (Equipment Modifier Layer): `StatModifier.deltas` is an untyped Dictionary. Typed loop `for stat_id: StringName in modifier.deltas` will crash at runtime if non-StringName keys are passed (e.g. int). Mitigate by using `Dictionary[StringName, float]` typed dict in a future refactor. — tracked from `production/epics/stat-system/story-003-equipment-modifier-layer.md`
- **2026-05-30** (Stat Surface + StatSource Enum + Allow-List): TR-stat-001 registry entry has stale stat name list (`STR/VIT/DEX/INT/AGI/LUK/?`) that doesn't match the implemented 7-stat surface (`STR/DEX/VIT/MAX_HP/ATTACK_POWER/MOVE_SPEED/CRIT_CHANCE`). Requires `/architecture-review` to sync. — tracked from `production/epics/stat-system/story-002-stat-surface-enum-allow-list.md`

- **2026-05-29** (Story 003 — Atomic Flush): **`Object.set(DIRTY_VAR_NAME)` test seam** — tests access `_dirty` via reflection `PersistenceLayer.set(&"_dirty", false)`. Add `_test_force_clean()` debug helper in Story 005 (test spy) to eliminate string-name coupling. Effort: S. — tracked from `production/epics/persistence-layer/story-003-atomic-flush-path.md`

- **2026-05-29** (Story 003 — Atomic Flush): **Debounce coalescing not tested** — AC-04b uses `flush=true` (synchronous) instead of real timer fire to prove atomic single-blob pattern. Add `test_debounce_coalesces_writes` integration test (inject fake clock or use `await get_tree().create_timer()`) in Story 006 or integration pass. Effort: S. — tracked from `production/epics/persistence-layer/story-003-atomic-flush-path.md`

- **2026-05-29** (Story 002 — In-Memory Cache): **AC-02 FileAccess spy probe missing** — "zero file I/O" claim proved via implementation code path, not via observable FileAccess.open() call counter. Add MockFileFactory-based spy assertion in Story 003 when IFileFactory is wired into PersistenceLayer. Effort: S. — tracked from `production/epics/persistence-layer/story-002-in-memory-cache.md`

- **2026-05-29** (Story 002 — In-Memory Cache): **`_cache` accessed via reflection in tests** — `PersistenceLayer.get(&"_cache").clear()` in before_each() is a reflection hack. Introduce `_clear_cache_for_testing()` debug helper in Story 005 (test spy contract) when the test infrastructure layer is solidified. Effort: S. — tracked from `production/epics/persistence-layer/story-002-in-memory-cache.md`

- **2026-05-29** (Story 001 — Sync Interface No-Await CI): **EC-4 string literal false-positive** — `check_no_await_in_persistence.sh` awk script would false-positive on `"await"` inside GDScript string literals (e.g., `push_error("no await allowed")`). Acceptable for stub-only file at VS tier; requires token-aware parser or quote-aware grep when full implementation lands. Effort: S. — tracked from `production/epics/persistence-layer/story-001-sync-interface-no-await.md`

- **2026-05-29** (Story 001 — Sync Interface No-Await CI): **Missing positive control test** — `tools/ci/check_no_await_in_persistence.sh` has no automated test that injects a real `await` statement and verifies the script returns exit 1 (guards against CI script always returning PASS). Effort: S. — tracked from `production/epics/persistence-layer/story-001-sync-interface-no-await.md`
